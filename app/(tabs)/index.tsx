import { SafeAreaView } from 'react-native';
import TodoApp from '@/components/TodoTask';

const App = () => {
  return (
    <SafeAreaView>
      <TodoApp />
    </SafeAreaView>
  );
};

export default App;